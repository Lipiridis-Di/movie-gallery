// Единая точка чтения переменных окружения. Vite подставляет их из .env,
// переменная должна начинаться с VITE_, иначе Vite не даст доступ к ней из кода браузера —
// это защита от случайной утечки серверных секретов в клиентский бандл.

export const TMDB_API_KEY = import.meta.env.VITE_TMDB_API_KEY as string | undefined
